services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: adega-api
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8000
      - ConnectionStrings__DefaultConnection=Host=ep-rapid-forest-ams8xemm-pooler.c-5.us-east-1.aws.neon.tech;Database=neondb;Username=neondb_owner;Password=npg_OwuCLEAoh9G5;Ssl Mode=Require;Trust Server Certificate=true;
    ports:
      - "8000:8000"
    networks:
      - adega-network

networks:
  adega-network:
    driver: bridge